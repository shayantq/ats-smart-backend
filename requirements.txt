# ---- Core Framework (Async) ----
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ---- Data Validation & Settings ----
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# ---- Database / ORM / Migrations ----
sqlalchemy==2.0.35
asyncpg==0.30.0
alembic==1.13.2
greenlet==3.1.1

# ---- Security (Auth stack - JWT & Password Hashing) ----
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
slowapi==0.1.9

# ---- Environment Management ----
python-dotenv==1.0.1

# ---- Storage (فقط لازم اگر STORAGE_BACKEND=s3 در .env تنظیم شود) ----
boto3==1.35.36

# ---- Redis (کش + صف کارهای پس‌زمینه) ----
redis==5.0.8
rq==1.16.2

# ---- OCR Engine و استخراج متن رزومه ----
# نکته‌ی مهم: PyMuPDF/python-docx/Pillow/pytesseract همه پکیج پایتون هستند و با
# همین pip نصب می‌شوند، اما خودِ موتور Tesseract OCR یک نرم‌افزار سیستمی جداست
# و باید جداگانه نصب شود (بنگرید بخش «موتور OCR» در README).
pymupdf==1.24.11
python-docx==1.1.2
pillow==10.4.0
pytesseract==0.3.13

# ---- Testing ----
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
